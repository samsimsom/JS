console.log('--- weather.js ---')

class Weather {
  constructor() {
    this.apikey = ''
    this.city = city
    this.state = state
  }

  // Fetch weather from Api
  async getWeather() {
    const responce = await fetch(``)
  }
}
